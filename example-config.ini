[Planet]
name = Example Planet
link = http://example.com
cache_directory = ./example-cache
output_dir = ./example-output
log_level = INFO
feed_timeout = 20
items_per_page = 10
template_files = examples/simple-template.html.tmpl

# Parallel fetch configuration (NEW!)
# Use 'parallel' for concurrent fetching (default) or 'sequential' for one-at-a-time
fetch_mode = parallel

# Number of concurrent workers for parallel fetching (default: 10)
# Adjust based on your system and network capacity
parallel_workers = 10

[https://go.dev/blog/feed.atom]
name = Go Blog
