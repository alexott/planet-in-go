<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{.Name}}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            line-height: 1.6;
            color: #333;
        }
        header {
            border-bottom: 2px solid #0066cc;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }
        h1 {
            margin: 0;
            color: #0066cc;
        }
        .meta {
            color: #666;
            font-size: 0.9em;
            margin-top: 10px;
        }
        article {
            margin-bottom: 40px;
            border-bottom: 1px solid #eee;
            padding-bottom: 30px;
        }
        article h2 {
            margin-top: 0;
            margin-bottom: 10px;
        }
        article h2 a {
            color: #333;
            text-decoration: none;
        }
        article h2 a:hover {
            color: #0066cc;
            text-decoration: underline;
        }
        .entry-meta {
            color: #666;
            font-size: 0.9em;
            margin-bottom: 15px;
        }
        .channel {
            color: #0066cc;
            text-decoration: none;
        }
        .channel:hover {
            text-decoration: underline;
        }
        .content {
            margin-top: 15px;
        }
        .content img {
            max-width: 100%;
            height: auto;
        }
        .date-header {
            background: #f5f5f5;
            padding: 10px 15px;
            margin: 30px -15px 20px;
            border-left: 4px solid #0066cc;
            font-weight: bold;
            color: #333;
        }
        footer {
            margin-top: 50px;
            padding-top: 20px;
            border-top: 2px solid #0066cc;
            text-align: center;
            color: #666;
            font-size: 0.9em;
        }
        @media (max-width: 600px) {
            body {
                padding: 10px;
            }
            .date-header {
                margin-left: 0;
                margin-right: 0;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>{{.Name}}</h1>
        <p class="meta">
            {{if .OwnerName}}Maintained by {{.OwnerName}}{{end}}
            {{if .OwnerEmail}} &lt;{{.OwnerEmail}}&gt;{{end}}
            <br>
            Last updated: {{.Date}}
        </p>
    </header>

    <main>
        {{range .Items}}
            {{if .NewDate}}
            <div class="date-header">{{.Date}}</div>
            {{end}}

            <article>
                <h2><a href="{{.Link}}">{{.Title}}</a></h2>
                <p class="entry-meta">
                    {{if .Author}}by <strong>{{.Author}}</strong> • {{end}}
                    <a href="{{.ChannelLink}}" class="channel">{{.ChannelName}}</a>
                </p>
                <div class="content">
                    {{.Content}}
                </div>
            </article>
        {{end}}
    </main>

    <footer>
        <p>Generated by {{.Generator}}</p>
        <p><a href="{{.Link}}">{{.Name}}</a></p>
    </footer>
</body>
</html>

